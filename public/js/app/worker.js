onmessage = function( event ){

  // var pngs = [];
  // for (var i = 0; i < contexts.length; i++) {
  //   var img = contexts[i].toDataURL("image/png");
  //   pngs.push(img);
  // }
  
  postMessage( 'done' );
}